# EDA_project

The project implements three algorithms including K-means, spectral and GraphBasedSuperpixel. To run the evaluation, your can execute the following instruction.

## GraphBasedSuperpixel
```
python ./evaluation.py gsc 
```

## Kmeans
```markdown
python ./evaluation.py kmeans
```

## Spectral clustering

```markdown
python ./evaluation.py spectral
```